#include "shared/trees/red-black/tests/delete.h"
#include "posix/test-framework/test.h"

void testRedBlackTreeDeletes(Arena scratch) {
    TEST_TOPIC(STRING("Deletes")) {
        TEST(STRING("Null tree")) {
            if (1) {
                testSuccess();
            }
        }
    }
}
